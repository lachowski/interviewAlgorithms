package pl.mobigen.interviewAlgorithms;

/**
 * Implement function that can weave two queues into one. 
 * Both input queues can have different lengths and int that case should not insert null values.
 * Use only add, remove and peek methods provided by the queue.
 *
 * Examples:
 * Queue q1 = new Queue();
 * q1.add(1);
 * q1.add(2);
 * q1.add(3);
 * q1.add(4);
 * Queue q2 = new Queue();
 * q2.add(11);
 * q2.add(22);
 * Queue q3 = weave(q1, q2); -> [1, 11, 2, 22, 3, 4]
 */
public class WaveQueues {
	
	public static Queue wave(Queue q1, Queue q2) {
		throw new UnsupportedOperationException();
	}

}
